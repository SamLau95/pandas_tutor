# This project now uses uv for dependency management.
# Development dependencies are now defined in pyproject.toml under [project.optional-dependencies.dev]
#
# To install development dependencies, use:
#   uv sync --extra dev
#
# Or run the setup script:
#   ./setup_env.sh
#
# The following is kept for backwards compatibility:
-r requirements.txt

# Locked dependencies for reference (actual versions may vary)
# Use 'uv pip freeze' to see current locked versions
numpy>=1.24.4
pandas>=1.5.0
libcst==0.3.21
docopt==0.6.2
simplejson==3.17.5
types-simplejson==3.17.1
mypy==0.910
pympler==0.9
babypandas==0.1.6
prettyprinter==0.18.0
wheel
build
black==24.3.0
twine
